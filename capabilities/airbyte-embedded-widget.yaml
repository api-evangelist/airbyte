naftiko: 1.0.0-alpha2
info:
  label: airbyte-api — embedded_widget
  description: 'airbyte-api — embedded_widget. 3 operations. Lead operation: Airbyte List Organizations That a User Has Access
    to That Have Embedded Enabled.. Self-contained Naftiko capability covering one Airbyte business surface.'
  tags:
  - Airbyte
  - embedded_widget
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AIRBYTE_API_KEY: AIRBYTE_API_KEY
capability:
  consumes:
  - type: http
    namespace: airbyte-embedded-widget
    baseUri: https://api.airbyte.com/v1
    description: airbyte-api — embedded_widget business capability. Self-contained, no shared references.
    resources:
    - name: embedded-organizations
      path: /embedded/organizations
      operations:
      - name: listembeddedorganizationsbyuser
        method: GET
        description: Airbyte List Organizations That a User Has Access to That Have Embedded Enabled.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: embedded-scoped_token
      path: /embedded/scoped_token
      operations:
      - name: generateembeddedscopedtoken
        method: POST
        description: Airbyte Generate a Scoped Token for Airbyte Embedded
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: embedded-widget_token
      path: /embedded/widget_token
      operations:
      - name: getembeddedwidget
        method: POST
        description: Airbyte Get a Widget Token for Airbyte Embedded
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: airbyte-embedded-widget-rest
    port: 8080
    description: REST adapter for airbyte-api — embedded_widget. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/embedded/organizations
      name: embedded-organizations
      description: REST surface for embedded-organizations.
      operations:
      - method: GET
        name: listembeddedorganizationsbyuser
        description: Airbyte List Organizations That a User Has Access to That Have Embedded Enabled.
        call: airbyte-embedded-widget.listembeddedorganizationsbyuser
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/embedded/scoped-token
      name: embedded-scoped-token
      description: REST surface for embedded-scoped_token.
      operations:
      - method: POST
        name: generateembeddedscopedtoken
        description: Airbyte Generate a Scoped Token for Airbyte Embedded
        call: airbyte-embedded-widget.generateembeddedscopedtoken
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/embedded/widget-token
      name: embedded-widget-token
      description: REST surface for embedded-widget_token.
      operations:
      - method: POST
        name: getembeddedwidget
        description: Airbyte Get a Widget Token for Airbyte Embedded
        call: airbyte-embedded-widget.getembeddedwidget
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: airbyte-embedded-widget-mcp
    port: 9090
    transport: http
    description: MCP adapter for airbyte-api — embedded_widget. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: airbyte-list-organizations-that-user
      description: Airbyte List Organizations That a User Has Access to That Have Embedded Enabled.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: airbyte-embedded-widget.listembeddedorganizationsbyuser
      outputParameters:
      - type: object
        mapping: $.
    - name: airbyte-generate-scoped-token-airbyte
      description: Airbyte Generate a Scoped Token for Airbyte Embedded
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: airbyte-embedded-widget.generateembeddedscopedtoken
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: airbyte-get-widget-token-airbyte
      description: Airbyte Get a Widget Token for Airbyte Embedded
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: airbyte-embedded-widget.getembeddedwidget
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
