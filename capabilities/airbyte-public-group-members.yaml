naftiko: 1.0.0-alpha2
info:
  label: airbyte-api — public_group_members
  description: 'airbyte-api — public_group_members. 3 operations. Lead operation: Airbyte List Group Members. Self-contained
    Naftiko capability covering one Airbyte business surface.'
  tags:
  - Airbyte
  - public_group_members
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AIRBYTE_API_KEY: AIRBYTE_API_KEY
capability:
  consumes:
  - type: http
    namespace: airbyte-public-group-members
    baseUri: https://api.airbyte.com/v1
    description: airbyte-api — public_group_members business capability. Self-contained, no shared references.
    resources:
    - name: groups-groupId-members
      path: /groups/{groupId}/members
      operations:
      - name: listgroupmembers
        method: GET
        description: Airbyte List Group Members
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: integer
          description: Set the limit on the number of members returned. The default is 20.
        - name: offset
          in: query
          type: integer
          description: Set the offset to start at when returning members. The default is 0.
      - name: addgroupmember
        method: POST
        description: Airbyte Add Member to Group
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: groups-groupId-members-userId
      path: /groups/{groupId}/members/{userId}
      operations:
      - name: removegroupmember
        method: DELETE
        description: Airbyte Remove Member from Group
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: airbyte-public-group-members-rest
    port: 8080
    description: REST adapter for airbyte-api — public_group_members. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/groups/{groupid}/members
      name: groups-groupid-members
      description: REST surface for groups-groupId-members.
      operations:
      - method: GET
        name: listgroupmembers
        description: Airbyte List Group Members
        call: airbyte-public-group-members.listgroupmembers
        with:
          limit: rest.limit
          offset: rest.offset
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: addgroupmember
        description: Airbyte Add Member to Group
        call: airbyte-public-group-members.addgroupmember
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/groups/{groupid}/members/{userid}
      name: groups-groupid-members-userid
      description: REST surface for groups-groupId-members-userId.
      operations:
      - method: DELETE
        name: removegroupmember
        description: Airbyte Remove Member from Group
        call: airbyte-public-group-members.removegroupmember
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: airbyte-public-group-members-mcp
    port: 9090
    transport: http
    description: MCP adapter for airbyte-api — public_group_members. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: airbyte-list-group-members
      description: Airbyte List Group Members
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: airbyte-public-group-members.listgroupmembers
      with:
        limit: tools.limit
        offset: tools.offset
      outputParameters:
      - type: object
        mapping: $.
    - name: airbyte-add-member-group
      description: Airbyte Add Member to Group
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: airbyte-public-group-members.addgroupmember
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: airbyte-remove-member-group
      description: Airbyte Remove Member from Group
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: airbyte-public-group-members.removegroupmember
      outputParameters:
      - type: object
        mapping: $.
